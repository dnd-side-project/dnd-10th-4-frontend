import { css } from '@emotion/react';

const styles = {
  navbar: css`
    display: flex;
    gap: 0.5rem;
    justify-content: center;
    width: 100%;
    padding: 1rem;
  `,
};

export default styles;
