import { css } from '@emotion/react';

const style = {
  input: css`
    display: none;
  `,
  iconContainer: css`
    flex-grow: 0;
    margin-left: 0.75rem;
    padding: 0.5rem 0.75rem;
    border-radius: 0.5rem;
  `,
};

export default style;
