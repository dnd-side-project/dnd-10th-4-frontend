import { css } from '@emotion/react';

const styles = {
  headerButton: css`
    display: flex;
    gap: 0.25rem;
    justify-content: center;
    align-items: center;
    cursor: pointer;
    user-select: none;
  `,
};

export default styles;
